name: clerk
version: 0.3.22
description: >-
  A command-based state manager. Clerk can properly compose small
  logic parts to improve readability and reusability of state
  management in your project.
environment:
  sdk: '>=2.6.0 <3.0.0'
dev_dependencies:
  mockito: ^4.1.1
  pedantic: ^1.9.0
  test: ^1.14.7
